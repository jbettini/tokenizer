

# solana config get/

docker-compose run --rm tokenizer /bin/bash

anchor test --skip-build --skip-deploy

solana program show [PROGRAM ID]
solana program extend [PROGRAM ID] 100000

yarn add --dev ts-mocha typescript @types/node @types/mocha && yarn add @solana/spl-token && yarn install

docker system prune -a --volumes
